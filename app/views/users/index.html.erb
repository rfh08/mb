<% provide(:title, 'All Coordinators') %>
<h5>All Coordinators</h5>

<ul class="nav nav-tabs">
    <li> <%= link_to "Courses", courses_path %> </li>
    <li> <%= link_to "Coordinators", users_path %> </li>
</ul>
<br>
<ul class="users">
  
    <% @users.each do |user| %>
    <div class="row">
    <div class="col-md-6 col-md-offset-3">
    <div class="mid">
    <li>

      <%= gravatar_for user, size: 150 %><br>
    <p4><%= link_to user.name, user %></p4>
        
    </li>
    </div>
    </div>
    </div>
  <% end %>

</ul>
